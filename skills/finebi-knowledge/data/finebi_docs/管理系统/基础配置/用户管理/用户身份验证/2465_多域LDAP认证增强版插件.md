---
title: 多域LDAP认证增强版插件
doc_id: 2465
url: https://help.fanruan.com/finebi6.X/doc-view-2465.html
source: FineBI 帮助文档
crawled_at: 2026-07-02 15:08:23
---

> 1. 概述1.1 版本报表服务器版本插件版本功能变更6.0V1.7.3基于原同步 LDAP 域用户插件，新增支持从多域同步以及 LDAPS6.0V1.7.5新增支持同步时重新同步用户类型，优化同步速度，

![](./view/2022finebi6/images/info_fill.png) 您正在浏览的是 FineBI6.X 帮助文档，点击跳转至： [FineBI7.X帮助文档](<https://help.fanruan.com/finebi/>)
# 多域LDAP认证增强版插件
对此内容反馈
* _
__
  * 使用此篇文档遇到问题，可寻求帆软技术支持协助定位原因，若为帆软产品本身缺陷则对应给出解决方案。  
详情：[《帆软技术支持基础服务范围》](<https://bbs.fanruan.com/thread-135893-1-1.html>)


产品级协助_
* * 文档创建者： _[Tracy.Wang](<user-space-2679113.html>)_
* 历史版本：[8](<edition-list-2465.html>)
* 最近更新：[Tracy.Wang](<user-space-2679113.html>) 于 2025-08-22 
[](<javascript:;>) [](<javascript:>)
## 1\. 概述
### 1.1 版本
  

报表服务器版本| 插件版本| 功能变更  
---|---|---  
6.0| V1.7.3  
| 基于原同步 LDAP 域用户插件，新增支持从多域同步以及 LDAPS  
6.0| V1.7.5| 新增支持同步时重新同步用户类型，优化同步速度，删除插件不会清空配置，支持部分特殊账号，支持多域重名用户同步，新增用户类型选择限制（当前用户类型配置需要同时勾选「平台用户」类型才能生效）  
6.0| V1.7.7| 新增适配集群  
6.0| V1.7.9| 新增支持维持用户名大小写不变  
6.0| V1.8.0| 新增支持同步用户属性扩展  
6.0| V1.8.3| LDAP 动态配置页面新增「节点名称」编辑框LDAP 同步页面新增显示「节点名称」，支持搜索名称LDAP 动态支持暂停同步用户类型支持使用字段  
6.0/6.1| V1.8.7| 优化 SSL认证证书验证（详情见本文 3.2 节）优化账号重复策略、账号转换策略体验过程（详情见本文 3.4 节）  
### 1.2 应用场景
用户信息存储在多个 LDAP 认证服务器中，管理员希望在数据决策系统中使用插件实现多域 LDAP 认证，并将 LDAP 服务器中的用户直接同步到数据决策系统中，而且相关的设置可以直接复用 LDAP 认证的配置。
  * 基于原同步 LDAP 域用户插件，新增支持从多个 LDAP 域同步用户。
  * 支持配置 LDAPS，并同步多域用户。
  * 支持单个 user 属于多个 group 的 AD 域结构（例如使用安全组），将 user 与 group 之间进行联动。


### 1.3 功能介绍
通过安装「多域LDAP认证增强版」插件，在进行「全局设置 > 认证方式」时，可以配置「多域LDAP认证」；在设置同步用户时，可以选择「从多个LDAP服务器中同步」；多域认证和同步用户均支持 LDAPS 。
注1：如果曾配置了「多域LDAP认证」插件，需删除旧插件后，重新安装「多域LDAP认证增强版」插件，并重新配置 LDAP 认证。
注2：如果在不同域存在相同账号的用户，使用各自的密码均可登录数据决策系统，但需要共享同一个平台账户。
注3：超级管理员不受 LDAP 认证的影响，依旧使用平台内置认证。
注4：暂不支持 PostgreSQL 外接数据库，如果有特殊需求请联系技术支持。
## 2\. 插件介绍
### 2.1 插件安装
点击下载插件：[多域Idap认证增强版](<https://market.fanruan.com/plugin/5a73f891-f60a-46f5-920f-f62febf262b9>)
设计器插件安装方法请参见：[设计器插件管理](<https://help.fanruan.com/finereport/doc-view-2198.html>)
服务器安装插件方法请参见：[服务器插件管理](<https://help.fanruan.com/finereport/doc-view-2220.html>)
### 2.2 插件简介
插件安装完成后，在进行「全局设置 > 认证方式」时，可以配置「多域LDAP认证」。
![](/core/style/lod.png)
![](/core/style/lod.png)
在同步用户时，用户来源可以选择「从多个LDAP服务器中同步」。如下图所示：
![726-1416.png](/core/style/lod.png)
## 3\. 示例
### 3.1 配置LDAP认证
管理员登录数据决策系统，点击「管理系统 > 用户管理 >全局设置」，同步用户的认证方式选择「多域LDAP认证」，点击添加即可在跳出的窗口填写配置信息。如下图所示：
多域 LDAP 认证的配置方法请参见：[多域LDAP认证](<https://help.fanruan.com/finebi6.X/doc-view-1188.html>)。
![](/core/style/lod.png)
参数填写完成后，点击「测试连接」。连接成功后，点击「保存」，即配置认证方式成功。
![](/core/style/lod.png)
全部新增完成后，点击右上角「保存」按钮，跳出数据决策系统，需重新登录。
### 3.2 配置LDAPS认证
3.1 节为一般的 LDAP 认证的配置方式，当需要用到 LDAPS 连接时，有以下内容需要注意
**1）节点名称**  

V1.8.3 及之后的版本支持配置「节点名称」：
![725-1359.png](/core/style/lod.png)
历史使用 V1.8.3 之前版本建立的 LDAP(S) 认证，「节点名称」显示为空。
节点名称不可重复，若重复则提示：节点名称已存在。
![725-1401.png](/core/style/lod.png)
**2）****URL 格式为：****Idaps://ip:port**
![725-1405.png](/core/style/lod.png)
**3）SSL认证 （点击按钮开启，非必须）**
注：SSL 认证只支持使用 JDK 内置证书。如果在 FineBI工程所在服务器成功安装认证，即成功配置系统信任认证，无需手动填写认证所在路径，Java 可自动读取已配置为信任的认证。手动填写的内容不会生效。
![725-1408.png](/core/style/lod.png)
### 3.3 进入同步用户编辑页面
**1）若管理员首次使用同步用户**
管理员登录数据决策系统，点击「管理系统 > 用户管理 > 所有用户」，然后点击「同步用户」。
弹出提示框「是否保留现有非同步数据，包括导入/新增的用户、部门职务、角色」，如下图所示：
![726-1424.png](/core/style/lod.png)
不同选择对应的更新逻辑如下表所示：
选择  
| 定义  
---|---  
保留| 如果现有用户不在同步数据集中，该用户信息和权限将被保留，不修改如果现有用户在同步用户来源中（账号相同）：
  * 该用户的账号不变，权限保留 
  * 该用户的姓名、密码、手机、邮箱将被改写更新
  * 该用户当前所属的部门职务、角色若存在于同步用户来源中，则改写更
  * 该用户当前所属的部门职务、角色若不存在于同步用户来源中，则保留不变

  
  
清空| 平台现有「手动新增/导入的用户」的账号、姓名、密码、手机、邮箱、部门、职位、角色、权限均被删除，重新同步用户  
注1：根据选择的更新逻辑，首次同步后有部分用户信息被更新。
注2：之后能够被自动更新的只有已变为同步类型的用户。
注3：之后的同步，数据集不能再重写更新内置数据，否则将会冲突报错。
**2）若管理员配置过同步用户**
管理员直接点击「同步用户管理」，选择「编辑」按钮，即可进入同步用户配置界面。
![](/core/style/lod.png)
### 3.4 配置同步用户
用户来源选择「从多个LDAP服务器中同步」，系统自动读取 3.1 节 多域LDAP 认证中的配置，并测试链接。 同步用户配置如下图所示：  

注：若之前配置过「同步用户」，且用户来源为「服务器数据集」，切换时会跳出提示「切换用户来源后将清空原有同步数据，包括用户及其部门、职务、角色、权限等，确认切换？」，点击「确定」，即可完成LDAP用户同步。
![](/core/style/lod.png)
其他配置项：  

配置项  
| 说明  
---|---  
同步频率  
| 支持通过两种方式执行同步用户作：简单重复执行、表达式设置**1）简单重复执行** 从 LDAP服务器中自动同步用户的间隔时间，默认为 43200 秒同步用户设定了同步频率，可自动进行多次同步，到达设置频率后自动进行同步，不断把 LDAP服务器中变化的数据同步到平台注：同步频率不宜过高，否则会导致后台日志不断重新整理，日志体积无限膨胀。**![](/core/style/lod.png)****2）表达式设置**  
支持通过 Cron 表达式设置任务运行的时间点，该任务可以是每天重复执行，隔日重复执行或者是单次执行等等各种不同组合的触发时间点Cron 表达式的文法和写法请参见：[Cron 表达式](<https://help.fanruan.com/finebi6.X/doc-view-89.html?source=1##9>)![](/core/style/lod.png)  
用户可编辑| 「用户可编辑」按钮默认不选中，选中后，同步状态下用户信息可编辑用户可编辑姓名、手机、邮箱，已存在用户的以上字段在自动同步/手动同步时将不再更新。 具体功能如下表所示：注：由于开启了同步用户LDAP认证，除了超管用户和内置用户外，所有的同步用户涉及密码相关的操作都是不可用的：包括设定加密方式、忘记密码、修改密码、重置密码等。| 用户身份  
| 说明  
---|---  
超级管理者| 1）再次同步时，平台现存用户的姓名、手机、邮箱字段将不再更新2）可编辑平台所有现存用户的姓名、手机、邮箱，不可编辑角色3）超管可在「账号设置」处编辑自己的姓名、密码、手机、邮箱4）可在登录页使用 [忘记密码](<https://help.fanruan.com/finereport/doc-view-2672.html#3>) 功能  
同步用户类型的次级管理者| 1）可修改有权限的同步用户的姓名、手机、邮箱，不可编辑角色、密码2）可修改有权限的内建用户的姓名、密码、手机、邮箱，不可编辑角色2）次管可在「账号设置」处编辑自己的姓名、手机、邮箱  
同步用户类型的普通使用| 同步用户可在「账号设置」处编辑自己的姓名、手机、邮箱  
账号重复策略| 支持2种处理方式：自动覆盖、提示重复信息并中断同步![](/core/style/lod.png)**特殊情况****：** 当设定多个同步任务（如任务A和任务B）来从不同的LDAP域（如域A和域B）中同步用户时，如果这些域中存在重复用户（假设为 User1 ），且不具有安全组结构，并且帐号重复策略选择「自动改写」，则在任务A暂停后， 同步任务B时会按以下逻辑处理重复用户（User1）的信息:
  * 如果域B在域A之上：域B中 User1 的信息会覆盖 FineDB 中原先从域A中同步的 User1 的信息
  * 如果域B在域A之下：保留任务A上次同步成功的 User1 的信息

注：仅支持 V1.8.7 及之后版本。  
账号转换策略| 1）V1.7.9 之前的版本同步用户时，会将账号统一转换成小写。2）V1.7.9 及之后的版本新增「账号转换策略」，支持 2 种策略：注1：无特殊需求建议不勾选此项，默认以小写形式存入数据库。注2：若同时勾选 2 种策略，效果等同于只勾选「转大写」。
  * 维持大小写不变：勾选后同步用户时，保持原账号大小写不变  


![](/core/style/lod.png)
  * 转大写：勾选后同步用户时，会将原账号转为大写

LDAP 内大小写不同的用户将会被视为同一个用户，同步时依照上一步中选定的「账号重复策略」进行自动覆盖或中断同步任务。当同步任务成功后：a）若外接数据库大小写敏感：用户需根据设定的「账号转换策略**」，** 使用转为大写/小写后的帐号进行登录决策系统。b）若外接数据库大小写不敏感：使用大写/小写账号均能登录决策系统。注：若外接数据库大小写敏感且历史同步过 LDAP 用户时，切换「账号转换策略」会导致同步失败，需要清空历史资料后重新同步。 修改配置后重新同步可能会导致部分用户配置权限丢失，例如：ANNA 原先同步时入库保存为 anna，勾选「维持大小写不变」后重新同步，该用户将保存为 ANNA，此时，原先分配给 anna 的角色配置将丢失，ANNA 无法继承 anna 的角色配置。  
启用同步LDAPURL| 1）可通过 URL 和节点名称对已添加的节点进行搜寻注：V1.8.3 之前的版本无此功能且不显示节点名称。![](/core/style/lod.png)2)点击按钮，即可编辑该域的同步属性。![](/core/style/lod.png)3）点击按钮，即可控制多个域的同步状态为「已开启」或「已禁用」。![](/core/style/lod.png)4）点击按钮，可控制同步任务的启动和暂停。
  * 禁用状态下点选“暂停同步”，提示：请先启动同步
  * 禁用状态下点选「启用同步」，提示：确定启动同步，点击确定后恢复到“已启动状态”，并正常同步该节点数据
  * 处于暂停状态的节点，每次同步时不会丢失之前已经同步成功的数据

注：V1.8.3 之前的版本无此功能。![](/core/style/lod.png)![](/core/style/lod.png)  
### 3.5 LDAP节点配置
#### 3.5.1 用户属性  

对于用户属性字段，需要先选择 ObjectClass，再选择 ObjectClass 内的属性值。
注：配置同步用户时，不需要配置密码，实际认证走 LDAP 密码认证。
![](/core/style/lod.png)
配置项| 说明  
| 是否必填  
---|---|---  
ObjectClass| 选择储存用户属性的 ObjectClass| 必填  
用户重复验证字段| 可选择通过帐号或用户ID进行用户重复验证1）若选择用户ID，则同步「用户ID 帐号」字段，所在表中的 ID 字段值为同步用户时LDAP服务器中的用户ID2）若选择帐号，则同步「帐号」字段，所在表中的用户ID 字段值将由系统随机生成| 必填  
用户ID| 仅在「用户重复验证字段」选择「用户ID」时需要填写选择用户属性中的 UID（用户ID）| 必填  
账号| 选择用户属性中的账号LDAP 服务器中储存的帐号，不可使用双字节日语、繁体中文或朝鲜文字符。 否则在平台登录时提示「账号或密码错误」| 必填  
用户类型| 共有 4 种预设类型：BI编辑用户、BI查看用户、平台用户、移动平台用户
  * V1.8.3 之前的版本需要手动输入对应属性值;V1.8.3及之后的版本可直接选择对应字段
  * V1.8.3 之前的版本，单节点只支持选择4种默认类型，不支持使用LDAP中的字段

![](/core/style/lod.png)
  * 如果用户类型值只有「BI编辑用户」或「BI查看用户」没有「平台用户」，则报错：当前用户类别配置需要同时勾选“平台用户”类别才能生效

![](/core/style/lod.png)
  * 如果有上述 4 种类型之外的值则报错：

![](/core/style/lod.png)
  * 用户类型支持多个值，在LDAP中可以用「，」或「; 」分隔，例如：platform,platform_mobile或 platform; platform_mobile

![](/core/style/lod.png)用户类型映射值说明：| 属性| 值  
| 适用版本  
---|---|---  
BI编辑用户  
| bi_design| 6.0  
BI查看用户| bi_view| 6.0  
平台用户| platform| 6.0  
移动平台用户| platform_mobile| 6.0  
BI设计用户-数据分析用户| bi_design_analysis| 5.1  
BI设计用户-数据处理用户| bi_design_process| 5.1  
非必填  
姓名| 选择用户属性中的姓名| 必填  
手机| 选择用户属性中的手机号| 非必填  
邮箱| 选择用户属性中的邮箱| 非必填  
扩展内容| 选择需要同步的其他内容（支持多选）![](/core/style/lod.png)注：V1.8.0 及之后的版本支持该功能。 设定后数据将以 JSON 格式储存至FineDB 库 Fine_Extra_Properties 表中。| 非必  
#### 3.5.2 部门属性
对于部门属性字段，需要先选择 ObjectClass，再选择 ObjectClass 内的属性值。
部门属性可不配置，但若选择了 ObjectClass，则必须配置部门名称/部门ID。
注：若使用安全组作为部门进行同步，可忽略此部分配置。
![](/core/style/lod.png)
配置项| 说明| 是否必填  
---|---|---  
ObjectClass| 选择储存部门属性的 ObjectClass注：不支持部门属性和角色属性使用同一个 ObjectClass 的不同属性。| 非必填要么都不填、要么都填  
部门重复验证字段| 可选择通过部门名称或部门ID进行部门重复验证。1）若选择 ID，同步「ID 名称」字段，所在表中的 ID 字段值为同步用户时LDAP服务器中的 ID。2）若选择名称，则同步「名称」字段，所在表中的 ID 字段值将由系统随机生成。  
部门ID| 仅在「部门重复验证字段」选择「部门ID」时需要填写选择部门属性中的UID（部门ID）  
部门名称| 选择部门属性中的部门名称  
#### 3.5.3 角色属性
对于角色属性字段，需要先选择 ObjectClass，再选择 ObjectClass 内的属性值。
角色属性可不配置，但若选择了 ObjectClass，则必须配置角色名称/角色ID。
![](/core/style/lod.png)
配置项| 说明| 是否必填  
---|---|---  
ObjectClass| 选择储存角色属性的 ObjectClass注：不支持部门属性和角色属性使用同一个 ObjectClass 的不同属性。| 非必填要么都不填、要么都填  
角色重复验证字段| 可选择透过角色名称或角色ID进行角色重复验证1）若选择 ID，同步「ID 名称」字段，所在表中的 ID 字段值为同步用户时LDAP服务器中的 ID2）若选择名称，则同步「名称」字段，所在表中的 ID 字段值将由系统随机生成  
角色ID| 仅在角色重复验证字段选择角色ID时需要填写选择角色属性中的UID（角色ID）  
角色名称| 选择角色属性中的角色名称  
#### 3.5.4 安全组属性（以安全组作为部门同步）  

在 Active Directory（AD）域中，安全组（Security Group）是一种用于管理和分配权限的对象。 安全组是 AD 中的一个重要概念，它允许管理者将一组用户或计算机归为一组，并向该组授予特定资源或对象的访问权限。
该项配置讲实现同步安全组 member 属性对应的用户。
注1：安全组属性不可和部门属性同时配置，否则提示：“部门属性和安全组属性不可同时配置”
注2：安全组属性不可和自订部门属性同时配置，否则提示：“自订部门属性和安全组属性不可同时配置”
一般适用的结构为：
![](/core/style/lod.png)
配置项如下：
![](/core/style/lod.png)
配置项| 说明| 是否必填  
---|---|---  
ObjectClass| 选择储存安全组（部门）属性的 ObjectClass选择 ObjectClass 后根据 ObjectClass 加载所有安全组（作为「组选择」对应的下拉复选框组件的选项）| 非必填要么都不填、要么都填  
组名称| 如果选择了「组名称」属性，则取组名称字段对应的 LDAP 属性值为「组选择」下拉选项的显示值| 非必填若不填，“组选择”下拉选项将默认读取cn  
组选择组与部门映射| 选择安全组之后，组部门映射自动加载映射表格，「部门名称」列支持手写输入| 非必填要么都不填、要么都填  
### 3.6 效果查看
若 LDAP 系统认证成功，且LDAP域用户同步成功。  

如图所示启用了两个 LDAP URL，点击确认后进行同步。
![](/core/style/lod.png)
同步成功提示，如下图：
![](/core/style/lod.png)
用户输入 LDAP 服务器中储存的账号、LDAP 服务器中的密码，即可进入数据决策系统，按照该用户在平台中的权限进行相应操作。如下图所示：
![](/core/style/lod.png)
![](/core/style/lod.png)
注1：若在平台中不存在输入账号，或平台中对应的用户被禁用，或开启了平台使用用户限制且不包含用户，则不与LDAP服务器进行通讯，直接提示「账号或密码错误」或「用户不可用」。
注2：LDAP服务器中储存的账号，不可使用双字节日语、繁体中文或朝鲜文字符。否则在平台登录时提示 「账号或密码错误」。
LDAP服务器中储存的密码，不可使用双字节日语、繁体中文、简体中文或朝鲜文字，否则在平台登录时提示「账号或密码错误」。
## 4.注意事项
### 4.1 特殊字符限制
**1）安全组名称**
在 AD 中建立安全组时禁用以下符号：/ \ [] : ; | = , + ? < > "
**2) 用户名称**
在 AD 中建立时禁用户用以下符号：/ \ [] : ; | = , + ? < > "**  
**
![](/core/style/lod.png)
综上，同步时支持的账号包含的特殊符号仅有： （ ） _ 空格  

**3）OU名称**
支持使用： / \ () _ ; , + * < > 空格
### 4.2 插件禁用删除
**1） v1.7.5版本及之后的版本  
**
插件删除后配置保留。
**2）v1.7.4版本及之前的版本**  

插件删除会删除已有配置，请谨慎删除并做好配置信息的备份。
配置链接相关信息存储在 FineDB 库 FINE_CONF_ENTITY 中。查询语句：SELECT * FROM FINE_CONF_ENTITY WHERE ID LIKE '%FINE_MULTI_LDAP VALUES%'
注：如删除重装插件后需恢复原有配置，需手动修改 FineDB 后重启，该作较危险，请联系技术支持协助进行，并务必保证作前做好备份工作。
### 附件列表 
  
下载次数：：0
    
**主题：** [管理系统](<category-view-100>)
[![](/core/style/back.png)上一篇：用户身份验证](<index.php?doc-view-1059.html>)
[下一篇：LDAP 认证 ![](/core/style/forward.png) ](<index.php?doc-view-1058.html>)
  * 有帮助
  * 没帮助
  * 只是浏览


[中文（繁體）](<https://help.fanruan.com/finebi-tw/>) [English](<https://help.fanruan.com/finebi-en/>) [日本語](<https://help.fanruan.com/finebi-jp/>)
中文（简体）


__
[](<javascript:void\(0\)>)
提交页面反馈
仅适用于当前网页的意见收集，帆软产品问题请在 [问答板块提问](<https://bbs.fanruan.com/wenda>) 或 [前往服务平台](<https://service.fanruan.com/>) 获取技术支持 
