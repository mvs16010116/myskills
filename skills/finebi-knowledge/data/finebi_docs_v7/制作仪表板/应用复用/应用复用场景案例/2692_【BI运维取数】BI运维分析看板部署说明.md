---
title: 【BI运维取数】BI运维分析看板部署说明
doc_id: 2692
url: https://help.fanruan.com/finebi/doc-view-2692.html
source: FineBI 7.X 帮助文档
crawled_at: 2026-07-16 17:20:37
version: "7.X"
---

> 1. 概述1.1 版本FineBI版本BI运维取数插件BI应用共享插件6.1.6及以上V2.2.7及以上V2.3.4及以上7.0.0及以上V2.2.7及以上V2.1.4及以上1.2 应用场景基于 Fin

![](./view/finebi70_2025/images/info_fill.png) 您正在浏览的是 FineBI7.X 帮助文档，点击跳转至： [FineBI6.X帮助文档](<https://help.fanruan.com/finebi6.X/>)
# 【BI运维取数】BI运维分析看板部署说明
[__](<doc-edit-2692.html>)
对此内容反馈
* _
__
  * 此方案由番薯贡献。  
若完全参照文档中场景与步骤操作，出现问题可咨询帆软技术支持团队，提供服务范围内的指导。（注：文档场景可能无法兼容所有客户场景）  
其他情况，可到帆软社区提问（问题响应快，解决率超80%），[立即提问](<https://bbs.fanruan.com/wenda>)。  
详情：[《关于帆软社区提问的相关说明》](<https://bbs.fanruan.com/thread-117166-1-1.html>)  
技术支持服务范围详见：[《帆软技术支持基础服务范围》](<https://bbs.fanruan.com/thread-135893-1-1.html>)


社区级协助_
* * 文档创建者： _[April陶](<user-space-431758.html>)_
* 历史版本：[42](<edition-list-2692.html>)
* 最近更新：[星踪](<user-space-80957.html>) 于 2026-07-08 
[](<javascript:;>) [](<javascript:>)
## 1\. 概述
### 1.1 版本
FineBI版本| BI运维取数插件| BI应用共享插件  
---|---|---  
6.1.6及以上| V2.2.7及以上| V2.3.4及以上  
7.0.0及以上| V2.2.7及以上| V2.1.4及以上  
  
### 1.2 应用场景
基于 FineBI 产品，我们识别到用户的IT人员在管理系统的过程中有较多的系统运维与推广的数据分析需求，例如：
  * 希望整体了解BI系统的情况，包括数据资产、权限、平台活跃等
  * 希望对数据更新任务做监控和分析，采取对应优化动作
  * 希望对长期无人使用的数据资产和账号进行清理，并根据血缘判断影响程度


为此，应用市场孵化了一款 “**帆软BI运维分析** ”的看板 ，客户在 FineBI 工程安装后，可分析BI系统数据，辅助企业进行系统运维与推广工作。
### 1.3 前置条件
1）BI工程版本6.1.6以上，有定时调度功能点，能够连接帆软市场所需要的网址资源，具体可见：[网址白名单](<https://help.fanruan.com/finebi/doc-view-2512.html#ad2c6faf21530839> "网址白名单")的1.3。
2）准备数据库：需要准备一个空的数据库用于存储系统数据，供看板使用。该数据库需要为mysql、sqlserver类型，且数据存储区分大小写，其他类型数据库可能会有语法兼容问题。数据库所在机器的最低配置建议：内存4G、CPU4核8线程、SSD磁盘剩余空间100G，如果企业BI使用体量大，则需要对应提高该资源配置。
3）下载并安装BI应用共享插件，6.1版本：「[BI应用共享](<https://market.fanruan.com/plugin/bb572248-abe0-459d-9917-ecf6c98652c2>)」，7.0版本：「[BI应用共享](<https://market.fanruan.com/plugin/6c281a9e-af01-4c4c-943b-4366117c04de>)[](<https://market.fanruan.com/plugin/bb572248-abe0-459d-9917-ecf6c98652c2>)」。
  

## 2\. 部署BI数据抽取插件
### 2.1 安装插件
#### 2.1.1 全新安装  

如果之前从未安装过「BI运维取数插件」，则直接在插件管理里安装即可。该插件初始化配置好后，会定期将BI工程本身相关的系统数据抽取到提前准备好的数据库中，用于后续的分析。
![icon](https://help.fanruan.com/core/style/lod.png)提示:「BI运维取数插件」
插件处于内测阶段，需联系对应技术支持或客户成功获取。
  

  

#### 2.1.2 原有插件升级
如果之前安装过「BI运维取数插件」的2.2.0以及更早版本，则除了更新插件外，因为新版本表结构和部分计算逻辑发生了变化，所以还需要做一些额外的处理。
1）备份原来已经抽取到数据库的所有相关表。
2）参考2.2.3调整「数据集更新事实表」和「用户行为事实表」的抽取时间配置。
3）删除原有表，参考2.3调整定时任务的公式，让插件重新建表和抽取数据。
4）参考3.1和3.2重新拉取表并设置定时数据更新，过程中检查下拉取的表字段是否完整。
![icon](https://help.fanruan.com/core/style/lod.png)提示: 「数据集更新事实表」和「用户行为事实表」的数据来源为logdb，如果logdb设置了定期清理， 则可能会缺少数据，此时视情况可以从备份中手动将以前的数据导入到表中。
如果之前安装过「BI运维取数插件」的2.2.2版本，则除了更新插件外，还需要给对应数据连接账号增加DROP权限。
### 2.2 数据连接配置
#### 2.2.1 新增数据连接
新建一个数据连接，对应提前准备好用于存储BI系统数据的数据库。连接该数据库的账号需要有CREATE、INSERT、UPDATE、DELETE、SELECT、DROP权限。因为给的权限较大，建议只给到单独的database上，避免权限扩大到其他database带来一定的风险。
![icon](https://help.fanruan.com/core/style/lod.png)提示:
【必做】如果是mysql数据库，需要在数据连接url后面增加参数rewriteBatchedStatements=true来开启批量提交，以减少抽数时间。
若 URL 中未添加过参数，在最后加 ?rewriteBatchedStatements=true
若 URL 中已添加过参数，在最后加 &rewriteBatchedStatements=true
![](https://help.fanruan.com/core/style/lod.png)
#### 2.2.2 绑定数据连接  

1）在系统管理界⾯找到「BI数据抽取配置」选项。
2）在「数据源」下拉框中选择2.2.1中设置好的数据连接，「模式」下拉框如果没有选项则不需要选择。
3）点击「保存」按钮完成配置。
![](https://help.fanruan.com/core/style/lod.png)
#### 2.2.3 配置事实表的抽取开始时间  

1）在「事实表抽取开始时间配置」页面中设置事实表的抽取开始时间。该时间决定了抽数插件从什么时间点开始读取系统数据，建议根据需要来设置，避免第一次抽取时因设置的时间过早导致的大数据量抽取性能问题。建议按照以下范围来设置：「数据集更新事实表」（最近2个月）、「用户行为事实表」（最近3个月）、「引擎性能分析表」（最近1个月）、其他表（默认）。
2）插件每次完成抽取后，都会自动将此处的时间更新到本次抽取的时间，下次抽取就会从本次抽取完成的时间点开始读取数据。如果后续遇到数据异常，需要清空表数据重新抽取，则需要手动修改此处时间。
3）[帆软BI运维分析](<https://app.fanruan.com/templates/20001779>)看板仅使用到了「数据集更新事实表」和「用户行为事实表」，其余表在[性能与稳定性监控看板](<https://app.fanruan.com/templates/20001779>)中被使用。
![](https://help.fanruan.com/core/style/lod.png)
### 2.3 配置定时调度任务
1）在「管理系统>定时调度」中点击「新建」创建定时任务。
2）参考下图配置任务，有几个关键配置信息：
  * 默认用户组：选择指定用户，用于接收数据抽取的结果信息
  * 执行频率：根据需要设置，建议固定为每天的凌晨运行，减少对工作时间的资源占用。如果平台用户设置了定时同步，则最好也避开同步用户触发的时间段，防止出现抽数期间用户信息改变的情况。
  * 处理⽅式：选择「平台通知」  

  * 内容：数据更新结果 ${updateBIData('fp_user','fp_department','fp_role','fp_authority_user_result','fp_update_task','fp_entry','fp_resource','fp_sp_usage','fp_task_execute','fp_user_behavior_detail')} 


![icon](https://help.fanruan.com/core/style/lod.png)提示:
【选做】fp_data_field表在看板中未使用到，该表存储了公共数据的字段信息，数据量可能较大，会影响抽数时间。建议对该表有自定义分析的需求时，再考虑加上该表，将公式改为：${updateBIData('fp_user','fp_department','fp_role','fp_authority_user_result','fp_update_task','fp_entry','fp_resource','fp_sp_usage','fp_data_field','fp_task_execute','fp_user_behavior_detail')} 
![icon](https://help.fanruan.com/core/style/lod.png)提示:
【选做】如果需要同时使用[性能与稳定性监控看板](<https://app.fanruan.com/templates/20002032>)，则公式改为：
${updateBIData('fp_user','fp_department','fp_role','fp_authority_user_result','fp_update_task','fp_entry','fp_resource','fp_sp_usage','fp_task_execute','fp_user_behavior_detail','fp_engine_performance_details','fp_gc_message_details','fp_real_time_usage','fp_inspection_app_config','fp_inspection_env_details')} 
3）如果工程数据较多，该定时任务需要的时间会比较长（几个小时），过程中不要手动修改相关数据表避免产生脏数据。
4）如果执行失败，可以从日志或者消息通知中查看报错并调整，调整后只需要针对报错的表重新更新即可（updateBIData函数中只写报错表的表名即可单独更新该表）。如果无法找到报错或者对报错有疑问，请联系技术支持协助排查。
![](https://help.fanruan.com/core/style/lod.png)
![](https://help.fanruan.com/core/style/lod.png)
![](https://help.fanruan.com/core/style/lod.png)
![](https://help.fanruan.com/core/style/lod.png)
函数说明
![](https://help.fanruan.com/core/style/lod.png)
  

## 3\. 配置公共数据
### 3.1 拉取数据表
将系统数据抽取到数据库后，下一步则需要在BI的公共数据中加入这些数据表，供看板使用。在公共数据下新建一个文件夹，并拉取数据库中的11张数据表。相关数据表的数据字典参见：[BI运维数据字典](<https://help.fanruan.com/finebi/doc-view-2693.html>)。
![](https://help.fanruan.com/core/style/lod.png)  

【选做】如果数据库中的事实表（例如fp_task_execute和fp_user_behavior_detail）数据量太大影响公共数据更新性能，可以用sql表的方式导入，通过where条件控制表里的时间字段，根据需要过滤出最近的数据进行后续的分析。具体sql可参考：
sqlserver数据库：
[code]
    SELECT *  
    FROM fp_task_execute  
    WHERE startTime >= DATEADD(MONTH, DATEDIFF(MONTH, 0, GETDATE()) - 3, 0)  
    AND startTime <= GETDATE()  
    order by startTime
[/code]
[code]
    SELECT *  
    FROM fp_user_behavior_detail  
    WHERE operateTime >= DATEADD(MONTH, DATEDIFF(MONTH, 0, GETDATE()) - 3, 0)  
    AND operateTime <= GETDATE()  
    order by operateTime
[/code]
mysql数据库：
[code]
    SELECT *  
    FROM fp_task_execute  
    WHERE startTime >= DATE_FORMAT(DATE_SUB(NOW(), INTERVAL 3 MONTH), '%Y-%m-01')  
    AND startTime <= NOW()  
    order by startTime  
    
[/code]
[code]
    SELECT *  
    FROM fp_user_behavior_detail  
    WHERE operateTime >= DATE_FORMAT(DATE_SUB(NOW(), INTERVAL 3 MONTH), '%Y-%m-01')  
    AND operateTime <= NOW()  
    order by operateTime
[/code]
### 3.2 设置数据定时更新
对11张数据表设置定时全量更新，更新的频率和之前定时任务的频率保持一致，建议一天一次。更新的时间需要在定时任务完成后，否则最新一次周期的数据会有缺失，同时尽量避开业务繁忙的时间段，以免影响业务数据更新。  

建议先运行几次定时调度任务和数据更新任务看一下所需要的时间，再评估设置什么时间比较合适。例如定时调度任务需要3h，数据更新任务需要1h，那把定时调度任务设置在每天凌晨1点开始运行，数据更新任务设置在每天早上6点开始运行会比较稳妥。
![](https://help.fanruan.com/core/style/lod.png)
  

  

## 4\. 安装模板
1）在模板市场找到帆软BI运维分析看板，点击「一键应用」按钮。
![](https://help.fanruan.com/core/style/lod.png)
2）根据提示存到我的分析，等待模板创建成功后点击立即编辑。
![](https://help.fanruan.com/core/style/lod.png)
3）修改数据源
将所有【原始表】为前缀的数据逐个进行替换，更换为之前拉取好的公共数据中的对应表，对应关系可见下方表格。匹配字段默认都会自动匹配好，可以检查下是否有未匹配成功的字段，有的话需要手动选一下匹配字段。
![](https://help.fanruan.com/core/style/lod.png)
![](https://help.fanruan.com/core/style/lod.png)
![](https://help.fanruan.com/core/style/lod.png)
主题内数据集名称  
| 公共数据表名称  
---|---  
【原始表】用户信息| fp_user  
【原始表】部门信息| fp_department  
【原始表】角色信息| fp_role  
【原始表】权限结果| fp_authority_user_result  
【原始表】资源信息| fp_resource  
【原始表】资源血缘| fp_sp_usage  
【原始表】定时更新任务信息| fp_update_task  
【原始表】更新任务执行记录| fp_task_execute  
【原始表】用户行为记录| fp_user_behavior_detail  
4）替换完成之后，点击「更新」，更新成功后即可查看模板。
![](https://help.fanruan.com/core/style/lod.png)
## 5\. 特此声明
后续帆软官方FineBI 6.1、7.0版本的系统运维分析取数需求都通过本应用进行迭代更新，建议优先考虑本方案。
如果直接从finedb、logdb、接口等其他形式取数分析，可能会因为表结构变动、接口变动等原因导致分析不可用。
### 附件列表 
  
下载次数：：0
    
**主题：** [制作可视化组件](<category-view-569>)
[![](https://help.fanruan.com/core/style/back.png)上一篇：BI运维分析看板使用说明](<index.php?doc-view-2751.html>)
[下一篇：【BI运维取数】数据字典 ![](https://help.fanruan.com/core/style/forward.png) ](<index.php?doc-view-2693.html>)


__
[](<javascript:void\(0\)>)
提交页面反馈
仅适用于当前网页的意见收集，帆软产品问题请在 [问答板块提问](<https://bbs.fanruan.com/wenda>) 或 [前往服务平台](<https://service.fanruan.com/>) 获取技术支持 
